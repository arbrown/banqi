{
  "name": "banqi-monorepo",
  "private": true,
  "workspaces": [
    "app"
  ],
  "scripts": {
    "dev": "FIRESTORE_EMULATOR_HOST=localhost:8080 FIREBASE_AUTH_EMULATOR_HOST=localhost:9099 npm run dev --workspace=app",
    "emulate": "firebase emulators:start --import=./.firebase-emulator-data --export-on-exit=./.firebase-emulator-data"
  },
  "devDependencies": {
    "concurrently": "^8.2.1"
  }
}