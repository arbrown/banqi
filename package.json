{
  "name": "banqi-monorepo",
  "private": true,
  "workspaces": [
    "app"
  ],
  "scripts": {
    "dev": "FIREBASE_AUTH_EMULATOR_HOST=\"localhost:9099\" concurrently \"firebase emulators:start\" \"npm run dev --workspace=app\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
